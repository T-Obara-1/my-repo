name: Repository variables
on: push
jobs:
    print:
        runs-on: ubuntu-latest
        env:
            USERNAME: ${{ vars.USERNAME}} # 環境変数USERNAMEの参照
        steps:
            - run: echo "${USERNAME}"